# Dock 悬停窗口预览 P3 窗口操作增强人工验收清单

日期：2026-07-06

## 状态

P3 窗口操作增强已完成实现和自动测试；人工验收尚未执行。本文只记录真实执行结果，未执行项保持 `not run`，不提前写成通过。

## 自动验证覆盖

- 右键菜单动作模型：激活窗口、隐藏应用、关闭窗口、最小化窗口。
- 右键菜单只读屏幕提示和禁用项行为。
- 窗口操作服务公开接口路径：`NSRunningApplication.hide()`、公开辅助功能按钮、公开辅助功能属性。
- 关闭/最小化失败降级和日志结果。
- 菜单跟踪期间会话保留，菜单结束后恢复离开判断，覆盖离开轮询和 Dock 悬停丢失路由。
- 屏幕匹配 helper：单屏、多屏、未知屏幕。
- 屏幕录制权限缺失路径仍不显示预览面板。

## 最终自动验证记录

- `swift test`：2026-07-06 CST，164 XCTest，0 failures，exit 0。
- `swift build`：2026-07-06，exit 0。
- `Scripts/build_probe_app.sh`：2026-07-06，exit 0，输出 `/Users/zong/Desktop/Project/zongMacTools/build/zongMacTools.app`，Info.plist OK，替换 existing signature。
- `git diff --check`：2026-07-06，exit 0。

## 人工验收清单

| 项目 | 检查内容 | 结果 | 记录 |
| --- | --- | --- | --- |
| [ ] 底部程序坞 | 右键菜单四个操作可见，主点击仍激活窗口。 | not run |  |
| [ ] 左/右程序坞 | 右键菜单不越界，打开菜单时预览面板不会提前消失。 | not run |  |
| [ ] 程序坞自动隐藏 | 菜单打开期间不会因为鼠标离开卡片而丢失操作上下文。 | not run |  |
| [ ] 台前调度 | 有真实窗口时操作正常；占位卡片没有真实窗口元素时关闭/最小化禁用或失败安静。 | not run |  |
| [ ] 浅色/深色外观 | 菜单和预览面板可读，屏幕提示可见。 | not run |  |
| [ ] 减少动态效果 | 窗口操作后隐藏面板不出现旧面板残留。 | not run |  |
| [ ] 屏幕录制权限缺失 | 不显示预览面板，也不会出现右键菜单。 | not run |  |
| [ ] 系统辅助功能权限缺失 | 不启动程序坞监听，不弹出重复提示。 | not run |  |
| [ ] VS Code | 抽样验证激活、隐藏应用、关闭窗口、最小化窗口。 | not run |  |
| [ ] Chrome | 抽样验证激活、隐藏应用、关闭窗口、最小化窗口。 | not run |  |
| [ ] Typora | 抽样验证激活、隐藏应用、关闭窗口、最小化窗口。 | not run |  |
| [ ] IINA | 抽样验证激活、隐藏应用、关闭窗口、最小化窗口。 | not run |  |
| [ ] WPS | 抽样验证激活、隐藏应用、关闭窗口、最小化窗口。 | not run |  |

## 受限项

- 多显示器：当前硬件环境不可用，P3 屏幕提示的多显示器真实行为仍需后续硬件验证。

## 记录边界

- 本清单不复制、翻译或机械改写外部项目源码、文件结构、helper、注释或私有接口封装。
- 未人工执行的项目保持 `not run`。
