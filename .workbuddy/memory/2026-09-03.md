# 2026-09-03 工作日志

## website 官网按液态玻璃设计稿重构
- 依据画布设计稿 `ardot.tencent.com/file/721460737359045`（Landing Page · Liquid Glass）重构 `website/`：
  - `index.html`：重写为设计稿 8 板块结构（玻璃导航 / Hero+产品模拟图 / 两大核心功能 / 迁移叙事 / 特性网格 / FAQ / 下载 CTA / Footer），Hero 模拟图用纯 CSS 还原窗口+Dock+预览弹出框。
  - `styles.css`：完整实现设计 tokens（#F9FAFC 底、极光四色光晕、玻璃材质白 55%-72% + 白描边 + backdrop blur、设计稿阴影参数、macOS 系统字体栈、999/32/24/20/16/12 圆角体系）+ 响应式（1100/720 两档断点）。
  - `app.js`：精简为只做 GitHub Release 下载按钮接线（复用 `release-state.js`）；移除了设计稿中不存在的双语切换和分步演示逻辑。
- 设计稿没有的两个真实录屏（quick-look-recording.mp4、right-click-extension.mp4）保留在 assets/ 未引用。
- 已用 Playwright 截图验证桌面 1440 与移动端 390 渲染正常；验证脚本已删除。
- 旧网站的中/EN 语言切换、交互演示、录屏板块按设计稿移除。

## 新增 Hero 右键助手模拟图
- 在画布设计稿 Hero 区域新增 `Hero RightClick Mock`（24:108），位于 Dock 预览图正下方：
  - 1120×480 玻璃大卡片（白 50% + 投影），标题栏「zongMacTools — 右键新建文件」
  - 绿→蓝→紫渐变桌面 + 左侧半透明 Finder 装饰窗口
  - 主菜单：新建文件夹、显示简介、显示›、排序方式›、「新建文件」蓝色高亮
  - 子菜单：TXT / Markdown / JSON（蓝色高亮+白色方块）/ Word（蓝）/ Excel（绿）/ PPT（橙）
- 前端代码同步更新：
  - `index.html`：在 Hero 内插入 `.hero-mock-rightclick` 结构
  - `styles.css`：新增 `.rc-desktop`、`.rc-menu`、`.rc-submenu`、`.rc-menu-active`、`.rc-file-dot-*` 等样式，移动端隐藏背景装饰窗口并缩小菜单
